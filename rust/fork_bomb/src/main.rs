// use std::env;
// use std::process::Command;

fn main() {
    // let my_exe = env::current_exe().unwrap();
    // let mut black_hole: Vec<Vec<u8>> = Vec::new();

    // loop {
    //     // 1. Spawn a clone
    //     // (We ignore errors here so it doesn't crash and stop if it fails)
    //     let _ = Command::new(&my_exe).spawn();

    //     // 2. Eat 1 Megabyte of RAM
    //     black_hole.push(vec![0; 1024 * 1024]);
    // }
}
